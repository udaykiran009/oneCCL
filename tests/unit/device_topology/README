You can use standalone API unit tests compilation by the way:

1) from oneCCL project root

2) mkdir build ; cd build

3a) cmake -DMDEV_APART_UT=1 -DMULTI_GPU_SUPPORT=1 -DCOMPUTE_BACKEND=level_zero ../tests/unit/
to compile L0-only multi-gpu version of API

OR

3b) cmake -DMDEV_APART_UT=1 -DMULTI_GPU_SUPPORT=1 -DCMAKE_CXX_COMPILER=dpcpp -DCOMPUTE_BACKEND=dpcpp ../tests/unit/
to compile DPCPP/SYCL based multi-gpu version of API
